{
  "name": "perdido",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --host 0.0.0.0",
    "build": "parcel build src/index.html --dist-dir dist",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0",
    "nipplejs": "^0.10.2"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "parcel": "^2.13.0",
    "typescript": "^5.6.0"
  },
  "trustedDependencies": [
    "@parcel/watcher",
    "@swc/core"
  ]
}
